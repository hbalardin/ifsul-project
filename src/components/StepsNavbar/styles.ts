import styled from 'styled-components';

export const StepsNavbarContainer = styled.nav`
  ul {
    display: flex;
    align-items: center;

    gap: 8px;

    li {
      list-style: none;
    }
  }
`;
